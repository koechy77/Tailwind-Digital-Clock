// literal method 
let array = [12, 13, 14]
// console.log(array)

// array constructor
let array2 = new Array(2, 3, 4)
// console.log(array2)

// basic ops in an array 
// iterating through an array
// array2.forEach((value, index) => {
    // console.log(`value:${value}\tindex:${index}`)
// });

// length of an array
// console.log(`The length of the array is ${array.length}`)

// console.log(array2[1])
// console.log(array[array.length - 1])

// inserting element / value into an array
// push & unshift
array.unshift("rice")
array.push(74)
// console.log(array)

// removing elements from an array
array.shift()
array.pop()
array.splice()
// console.log(array)




